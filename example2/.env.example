STATIC_UPLOAD_SERVER_URL=...
STATIC_UPLOAD_SERVER_TOKEN=...

# Email notifications (optional — only needed if config.json has a "notify" block)
NOTIFY_TO=ops@example.com
NOTIFY_FROM=file-relay@example.com
SMTP_HOST=smtp.example.com
SMTP_USER=...
SMTP_PASS=...
